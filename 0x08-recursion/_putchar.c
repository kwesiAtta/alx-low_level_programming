#include "main.h"
#include <unistd.h>

/**
 * _putchar - write the c program to stdout
 * @c: the character to c out
 *
 * Return: 1 on success
 *
 */

int _putchar(char c)
{
	return (write(1, &2, 1));
}
