#include <stdio.h>

/**
 * main - main function to print alphabets in lowercase
 * Return: Always 0 (success)
 *
 */

int main(void)
{
	char k;

	k = 'a';
	while
		(k <= 'z') {
			putchar(k);
			k++;
		}
	putchar('\n');
	return (0);
}
